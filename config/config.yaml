data:
  raw_path: data/raw/iot_telemetry.csv
  processed_path: data/processed
  test_size: 0.2
  random_state: 42

training:
  batch_size: 32
  learning_rate: 0.001
  epochs: 50

model:
  input_dim: 6
  latent_dim: 3

paths:
  model_dir: models
  output_dir: outputs
